{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/simootaz/pantheon-aiops/core/contracts/export/pantheon.schema.json",
  "title": "PantheonContracts",
  "description": "Generated from core/contracts/ by codegen/export_schemas.py. Do not edit by hand.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "a2_u_i_client_capabilities": {
      "$ref": "#/$defs/A2UIClientCapabilities"
    },
    "a2_u_i_surface": {
      "$ref": "#/$defs/A2UISurface"
    },
    "access_request": {
      "$ref": "#/$defs/AccessRequest"
    },
    "action": {
      "$ref": "#/$defs/Action"
    },
    "agent_manifest": {
      "$ref": "#/$defs/AgentManifest"
    },
    "audit_entry": {
      "$ref": "#/$defs/AuditEntry"
    },
    "credential_ref": {
      "$ref": "#/$defs/CredentialRef"
    },
    "event_envelope": {
      "$ref": "#/$defs/EventEnvelope"
    },
    "evidence": {
      "$ref": "#/$defs/Evidence"
    },
    "finding": {
      "$ref": "#/$defs/Finding"
    },
    "grant": {
      "$ref": "#/$defs/Grant"
    },
    "investigation": {
      "$ref": "#/$defs/Investigation"
    },
    "lease": {
      "$ref": "#/$defs/Lease"
    },
    "model_descriptor": {
      "$ref": "#/$defs/ModelDescriptor"
    },
    "model_requirements": {
      "$ref": "#/$defs/ModelRequirements"
    },
    "provider_config": {
      "$ref": "#/$defs/ProviderConfig"
    },
    "resolution_record": {
      "$ref": "#/$defs/ResolutionRecord"
    },
    "root_cause_hypothesis": {
      "$ref": "#/$defs/RootCauseHypothesis"
    },
    "u_i_action_response": {
      "$ref": "#/$defs/UIActionResponse"
    },
    "verdict": {
      "$ref": "#/$defs/Verdict"
    }
  },
  "$defs": {
    "A2UIAction": {
      "additionalProperties": false,
      "description": "A declared action on a component.\n\nA2UI carries either a server event or a local function call, both referenced\n**by name**. No executable code crosses the boundary in either direction.",
      "properties": {
        "context": {
          "additionalProperties": true,
          "description": "Values returned with the action.",
          "title": "Context",
          "type": "object"
        },
        "event_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Server-dispatched action name, from the catalog.",
          "title": "Event Name"
        },
        "function_call": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Client function name, from the catalog. Never code.",
          "title": "Function Call"
        }
      },
      "title": "A2UIAction",
      "type": "object"
    },
    "A2UIClientCapabilities": {
      "additionalProperties": false,
      "description": "What the client can render, declared once at run start.\n\nA2UI carries this in A2A message metadata (`a2uiClientCapabilities`). AG-UI\ndefines no analog, so this is **Pantheon convention, not specification**: the\ndashboard sends it in the AG-UI run input, and the agent is told what it may\nemit before it emits anything.\n\n`components` is generated from A2UIComponentType, so what we advertise is\nexactly what the renderer accepts - there is no second list to keep in step.",
      "properties": {
        "a2ui_version": {
          "default": "v0.9.1",
          "title": "A2Ui Version",
          "type": "string"
        },
        "catalog_id": {
          "default": "pantheon.v1",
          "title": "Catalog Id",
          "type": "string"
        },
        "components": {
          "description": "Every component the renderer accepts.",
          "items": {
            "$ref": "#/$defs/A2UIComponentType"
          },
          "title": "Components",
          "type": "array"
        }
      },
      "title": "A2UIClientCapabilities",
      "type": "object"
    },
    "A2UIComponent": {
      "additionalProperties": false,
      "description": "One component in a surface. Authored by an agent, rendered by the host.\n\nNote what is absent: no styling, no HTML, no script, and no identity fields.\n``icon_url`` and ``agent_display_name`` live on A2UISurface and are set by\nthe orchestrator, so an agent cannot present itself as another agent or as\nPantheon itself.",
      "properties": {
        "action": {
          "anyOf": [
            {
              "$ref": "#/$defs/A2UIAction"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "artifact_ref": {
          "anyOf": [
            {
              "$ref": "#/$defs/ArtifactRef"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For Image. A reference Pantheon resolves; never a URL an agent supplies."
        },
        "children": {
          "description": "Child component ids.",
          "items": {
            "type": "string"
          },
          "title": "Children",
          "type": "array"
        },
        "component": {
          "$ref": "#/$defs/A2UIComponentType",
          "description": "Must be in the allowlist."
        },
        "data_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "RFC 6901 JSON Pointer into the surface data model.",
          "title": "Data Path"
        },
        "id": {
          "description": "Unique within its surface.",
          "title": "Id",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Input label, where the type takes one.",
          "title": "Label"
        },
        "text": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Display text, where the type takes one.",
          "title": "Text"
        }
      },
      "required": [
        "id",
        "component"
      ],
      "title": "A2UIComponent",
      "type": "object"
    },
    "A2UIComponentType": {
      "description": "The closed allowlist of A2UI components Pantheon will render.\n\nA subset of A2UI's basic catalog. Agent-generated UI is untrusted data, so\nthe catalog is chosen for what it *cannot* be abused to do.\n\n``Image`` is present but **cannot take a URL**. It takes an ArtifactRef: an\nobject key for an artifact Pantheon itself produced and stored. The agent\ncannot express an arbitrary destination, so there is nothing to filter.\n\nDeliberately excluded, with reasons:\n\n- ``Video``, ``AudioPlayer`` - nothing needs them yet. They would follow the\n  same ArtifactRef pattern when something does; the allowlist grows on\n  demand, never speculatively.\n- ``Modal`` - an agent that can force a modal can overlay a convincing fake\n  credential prompt. Credential requests travel one path only, through\n  Cerberus.\n- ``Tabs``, ``Slider`` - no current use.",
      "enum": [
        "Row",
        "Column",
        "Card",
        "List",
        "Text",
        "Image",
        "Icon",
        "Divider",
        "TextField",
        "CheckBox",
        "ChoicePicker",
        "DateTimeInput",
        "Button"
      ],
      "title": "A2UIComponentType",
      "type": "string"
    },
    "A2UISurface": {
      "additionalProperties": false,
      "description": "A renderable surface, assembled by Pantheon rather than by an agent.\n\nIdentity is set here, by the orchestrator, and never by the agent - A2UI\ncalls this out explicitly as an anti-impersonation measure.",
      "properties": {
        "a2ui_version": {
          "default": "v0.9.1",
          "title": "A2Ui Version",
          "type": "string"
        },
        "agent_display_name": {
          "default": "Pantheon",
          "description": "Set by the orchestrator. An agent cannot claim another identity.",
          "title": "Agent Display Name",
          "type": "string"
        },
        "catalog_id": {
          "default": "pantheon.v1",
          "title": "Catalog Id",
          "type": "string"
        },
        "components": {
          "items": {
            "$ref": "#/$defs/A2UIComponent"
          },
          "title": "Components",
          "type": "array"
        },
        "data_model": {
          "additionalProperties": true,
          "description": "Initial values bound by JSON Pointer.",
          "title": "Data Model",
          "type": "object"
        },
        "icon_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Set by the orchestrator. Never agent-supplied.",
          "title": "Icon Url"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Investigation Id"
        },
        "kind": {
          "$ref": "#/$defs/A2UISurfaceKind"
        },
        "root": {
          "description": "Id of the root component.",
          "title": "Root",
          "type": "string"
        }
      },
      "required": [
        "id",
        "kind",
        "root"
      ],
      "title": "A2UISurface",
      "type": "object"
    },
    "A2UISurfaceKind": {
      "description": "What a Pantheon-authored surface is for.\n\nEvery surface Pantheon emits has a declared purpose, so the renderer can\napply the right handling - notably that only APPROVAL and ACCESS_REQUEST may\ncollect a decision, and both are bound to their existing backend paths.",
      "enum": [
        "approval",
        "access_request",
        "report",
        "notice"
      ],
      "title": "A2UISurfaceKind",
      "type": "string"
    },
    "AccessRequest": {
      "additionalProperties": false,
      "description": "An agent asking for a capability, with the reason it is asking.\n\n`reason` is not decoration. Approving \"an agent wants database access\" is\nnot a decision; approving a stated hypothesis is.",
      "properties": {
        "action": {
          "$ref": "#/$defs/CredentialAction"
        },
        "agent": {
          "title": "Agent",
          "type": "string"
        },
        "credential_ref": {
          "$ref": "#/$defs/CredentialRef"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "reason": {
          "description": "The hypothesis this access would test, in the agent's own words.",
          "title": "Reason",
          "type": "string"
        },
        "requested_at": {
          "format": "date-time",
          "title": "Requested At",
          "type": "string"
        },
        "requested_ttl_seconds": {
          "description": "How long the agent expects to need it.",
          "exclusiveMinimum": 0,
          "title": "Requested Ttl Seconds",
          "type": "integer"
        }
      },
      "required": [
        "id",
        "investigation_id",
        "agent",
        "credential_ref",
        "action",
        "reason",
        "requested_ttl_seconds",
        "requested_at"
      ],
      "title": "AccessRequest",
      "type": "object"
    },
    "AccessRequestedEvent": {
      "additionalProperties": false,
      "description": "An agent asked Cerberus for a capability it has no standing grant for.",
      "properties": {
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "request": {
          "$ref": "#/$defs/AccessRequest"
        },
        "type": {
          "const": "access_requested",
          "default": "access_requested",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "request"
      ],
      "title": "AccessRequestedEvent",
      "type": "object"
    },
    "Action": {
      "additionalProperties": false,
      "description": "A remediation Pantheon proposes, and may later execute.",
      "properties": {
        "approval_state": {
          "$ref": "#/$defs/ApprovalState",
          "default": "pending"
        },
        "blast_radius": {
          "$ref": "#/$defs/BlastRadius"
        },
        "dry_run": {
          "default": true,
          "description": "Dry run until explicitly cleared.",
          "title": "Dry Run",
          "type": "boolean"
        },
        "execution_state": {
          "$ref": "#/$defs/ExecutionState",
          "default": "proposed"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "operation": {
          "description": "What it does, e.g. 'rollout_restart', 'scale'.",
          "title": "Operation",
          "type": "string"
        },
        "parameters": {
          "additionalProperties": true,
          "description": "Operation arguments, e.g. {'replicas': 4}.",
          "title": "Parameters",
          "type": "object"
        },
        "proposed_at": {
          "format": "date-time",
          "title": "Proposed At",
          "type": "string"
        },
        "proposed_by": {
          "description": "Agent codename, or 'zeus'.",
          "title": "Proposed By",
          "type": "string"
        },
        "reason": {
          "description": "Why this Action was proposed, in terms of the Verdict.",
          "title": "Reason",
          "type": "string"
        },
        "receipts": {
          "description": "Append-only execution history.",
          "items": {
            "$ref": "#/$defs/ActionReceipt"
          },
          "title": "Receipts",
          "type": "array"
        },
        "rollback": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How to undo it. Required for anything wider than a single workload.",
          "title": "Rollback"
        },
        "target": {
          "$ref": "#/$defs/ResourceRef"
        }
      },
      "required": [
        "id",
        "target",
        "operation",
        "blast_radius",
        "reason",
        "proposed_by",
        "proposed_at"
      ],
      "title": "Action",
      "type": "object"
    },
    "ActionReceipt": {
      "additionalProperties": false,
      "description": "What happened when an Action ran, and what let it. Written once, never amended.\n\n`decided_by` is required and cannot be empty. A receipt said what happened\nand never why it was allowed to: for a refusal the rule lived in an\nexception message, and for a success it was nowhere at all. \"Why did this\nrun\" is the first question asked afterwards, and the record could not\nanswer it.\n\nRequired rather than defaulted, so a receipt that cannot say is\nunconstructible - a default would be filled in by the one call site that\nforgot, which is the site that most needed to say.",
      "properties": {
        "approval_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The approval spent, when the rule required one. Null when it did not.",
          "title": "Approval Id"
        },
        "at": {
          "format": "date-time",
          "title": "At",
          "type": "string"
        },
        "connector": {
          "description": "Which connector executed it.",
          "title": "Connector",
          "type": "string"
        },
        "decided_by": {
          "description": "The policy rule that produced the decision, whichever way it went.",
          "minLength": 1,
          "title": "Decided By",
          "type": "string"
        },
        "detail": {
          "default": "",
          "description": "Human-readable outcome. Never a credential.",
          "title": "Detail",
          "type": "string"
        },
        "lease_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The lease it was executed under.",
          "title": "Lease Id"
        },
        "state": {
          "$ref": "#/$defs/ExecutionState"
        }
      },
      "required": [
        "at",
        "state",
        "connector",
        "decided_by"
      ],
      "title": "ActionReceipt",
      "type": "object"
    },
    "AgentAccounting": {
      "additionalProperties": false,
      "description": "What one agent's step consumed, beside what it was allowed.\n\nEVERY FIGURE CARRIES ITS CEILING\n----------------------------------\n\"spent 16000 tokens\" cannot answer the question anybody asks, which is\nwhether that was close to the limit. 16000 of 16384 and 16000 of 200000 are\ndifferent runs and the same number.\n\nThree resources because there are three ceilings on `AgentBudget`, and a\nbreakdown missing one cannot explain a DEGRADED step that hit it. A run\nstopped by its token budget and one stopped by its clock look identical from\nthe outside, and they are fixed differently.",
      "properties": {
        "agent": {
          "description": "Codename, e.g. 'argus'.",
          "title": "Agent",
          "type": "string"
        },
        "second_ceiling": {
          "default": 0,
          "minimum": 0,
          "title": "Second Ceiling",
          "type": "integer"
        },
        "seconds": {
          "default": 0.0,
          "minimum": 0.0,
          "title": "Seconds",
          "type": "number"
        },
        "token_ceiling": {
          "default": 0,
          "minimum": 0,
          "title": "Token Ceiling",
          "type": "integer"
        },
        "tokens_spent": {
          "default": 0,
          "minimum": 0,
          "title": "Tokens Spent",
          "type": "integer"
        },
        "tool_call_ceiling": {
          "default": 0,
          "minimum": 0,
          "title": "Tool Call Ceiling",
          "type": "integer"
        },
        "tool_calls": {
          "default": 0,
          "minimum": 0,
          "title": "Tool Calls",
          "type": "integer"
        }
      },
      "required": [
        "agent"
      ],
      "title": "AgentAccounting",
      "type": "object"
    },
    "AgentBudget": {
      "additionalProperties": false,
      "description": "Ceilings the dispatcher enforces for a single agent invocation.",
      "properties": {
        "max_seconds": {
          "exclusiveMinimum": 0,
          "title": "Max Seconds",
          "type": "integer"
        },
        "max_tokens": {
          "exclusiveMinimum": 0,
          "title": "Max Tokens",
          "type": "integer"
        },
        "max_tool_calls": {
          "exclusiveMinimum": 0,
          "title": "Max Tool Calls",
          "type": "integer"
        }
      },
      "required": [
        "max_tokens",
        "max_seconds",
        "max_tool_calls"
      ],
      "title": "AgentBudget",
      "type": "object"
    },
    "AgentCapability": {
      "additionalProperties": false,
      "description": "One thing an agent claims it can do.",
      "properties": {
        "description": {
          "title": "Description",
          "type": "string"
        },
        "name": {
          "description": "Stable identifier, e.g. 'detect_metric_anomaly'.",
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "description"
      ],
      "title": "AgentCapability",
      "type": "object"
    },
    "AgentManifest": {
      "additionalProperties": false,
      "description": "Everything the registry needs to know about an agent without importing it.",
      "properties": {
        "budget": {
          "$ref": "#/$defs/AgentBudget"
        },
        "capabilities": {
          "items": {
            "$ref": "#/$defs/AgentCapability"
          },
          "title": "Capabilities",
          "type": "array"
        },
        "codename": {
          "description": "Mythological name, e.g. 'argus'.",
          "title": "Codename",
          "type": "string"
        },
        "description": {
          "title": "Description",
          "type": "string"
        },
        "domain": {
          "description": "Folder under agents/, e.g. 'anomaly'.",
          "title": "Domain",
          "type": "string"
        },
        "tools": {
          "description": "Connector tools this agent may call.",
          "items": {
            "type": "string"
          },
          "title": "Tools",
          "type": "array"
        }
      },
      "required": [
        "codename",
        "domain",
        "description",
        "budget"
      ],
      "title": "AgentManifest",
      "type": "object"
    },
    "ApprovalRequestedEvent": {
      "additionalProperties": false,
      "description": "An Action needs a human before it can execute.",
      "properties": {
        "action": {
          "$ref": "#/$defs/Action"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "approval_requested",
          "default": "approval_requested",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "action"
      ],
      "title": "ApprovalRequestedEvent",
      "type": "object"
    },
    "ApprovalState": {
      "description": "Where an Action sits in the human-in-the-loop gate.",
      "enum": [
        "not_required",
        "pending",
        "approved",
        "rejected",
        "expired"
      ],
      "title": "ApprovalState",
      "type": "string"
    },
    "ArtifactKind": {
      "description": "What an artifact is. Only images are renderable today.",
      "enum": [
        "image"
      ],
      "title": "ArtifactKind",
      "type": "string"
    },
    "ArtifactRef": {
      "additionalProperties": false,
      "description": "A reference to an artifact Pantheon produced and stored. Never a URL.\n\nSame shape as CredentialRef, for the same reason: the agent names a thing it\nis allowed to name, and the server resolves it. A URL field would let an\nagent express an arbitrary destination, and the browser fetching it is a\ndata-exfiltration channel - the agent encodes what it learned into the URL\nand the browser delivers it.\n\nA server-side URL proxy was considered and rejected. It still accepts an\nagent-authored URL and defends by filtering, which is one bypass away from\nfailing. A reference has nothing to filter.\n\nNote what is absent: no URL, no host, no bucket. The bucket is fixed\nserver-side, so the agent cannot name one. Resolution happens only in\ncore.ui.artifact_resolution, which agents cannot import - the same boundary\nas core.cerberus.redemption.",
      "properties": {
        "alt_text": {
          "default": "",
          "description": "Accessible description. Rendered, not fetched.",
          "title": "Alt Text",
          "type": "string"
        },
        "investigation_id": {
          "description": "Resolution rejects a reference from a different investigation.",
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "key": {
          "description": "Object key within Pantheon's own artifact bucket.",
          "title": "Key",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/ArtifactKind",
          "default": "image"
        }
      },
      "required": [
        "key",
        "investigation_id"
      ],
      "title": "ArtifactRef",
      "type": "object"
    },
    "AuditEntry": {
      "additionalProperties": false,
      "description": "One immutable line in the credential audit log.\n\nAttached to the Investigation, which agents can see - safe because every\nreference here is a CredentialRef and never a value.\n\nFROZEN, and that is the whole of \"immutable\" above. The docstring said it\nfor two phases while assignment worked fine, so an append-only log rested on\na promise nothing enforced - and a trail that can be rewritten answers\nnothing. `tests/unit/test_audit_trail.py` plants the assignment.",
      "properties": {
        "action": {
          "$ref": "#/$defs/CredentialAction",
          "default": "not_applicable",
          "description": "NOT_APPLICABLE for events that concern no single access."
        },
        "actor": {
          "description": "Agent codename, user, or 'system'.",
          "title": "Actor",
          "type": "string"
        },
        "at": {
          "format": "date-time",
          "title": "At",
          "type": "string"
        },
        "credential_ref": {
          "anyOf": [
            {
              "$ref": "#/$defs/CredentialRef"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "detail": {
          "default": "",
          "description": "Human-readable context. Never a credential.",
          "title": "Detail",
          "type": "string"
        },
        "event": {
          "$ref": "#/$defs/AuditEvent"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Investigation Id"
        },
        "lease_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Lease Id"
        }
      },
      "required": [
        "id",
        "at",
        "event",
        "actor"
      ],
      "title": "AuditEntry",
      "type": "object"
    },
    "AuditEvent": {
      "description": "Everything Cerberus records. The log is append-only.",
      "enum": [
        "requested",
        "granted",
        "denied",
        "approval_requested",
        "lease_minted",
        "lease_used",
        "lease_renewed",
        "lease_expired",
        "lease_revoked",
        "grant_revoked",
        "break_glass",
        "rotated"
      ],
      "title": "AuditEvent",
      "type": "string"
    },
    "AuthMode": {
      "description": "How credentials are presented to a provider.",
      "enum": [
        "none",
        "bearer",
        "header_key",
        "query_param"
      ],
      "title": "AuthMode",
      "type": "string"
    },
    "BaselineEstimator": {
      "description": "How a baseline's centre and scale were computed.\n\nA closed set rather than free text, and deliberately so: an estimator field\ntyped `str` is where `median_mad`, `MAD` and `robust` all appear within a\nmonth, and nothing can compare two Findings after that. Every payload\ncarrying a baseline must name one - guarded by test.",
      "enum": [
        "median_mad",
        "mean_stddev",
        "not_applicable"
      ],
      "title": "BaselineEstimator",
      "type": "string"
    },
    "BlastRadius": {
      "description": "How much can break if this Action is wrong.",
      "enum": [
        "none",
        "single_workload",
        "namespace",
        "cluster",
        "multi_cluster"
      ],
      "title": "BlastRadius",
      "type": "string"
    },
    "BreakGlassEvent": {
      "additionalProperties": false,
      "description": "Every grant revoked and every live lease invalidated, immediately.\n\nThe one domain concept that becomes an AG-UI `Custom` event rather than a\nstate patch: it affects every run at once, so an open dashboard must react\non arrival rather than render a new audit row (ADR 0006).",
      "properties": {
        "audit_entry": {
          "anyOf": [
            {
              "$ref": "#/$defs/AuditEntry"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "invoked_by": {
          "title": "Invoked By",
          "type": "string"
        },
        "leases_revoked": {
          "default": 0,
          "minimum": 0,
          "title": "Leases Revoked",
          "type": "integer"
        },
        "reason": {
          "title": "Reason",
          "type": "string"
        },
        "type": {
          "const": "break_glass",
          "default": "break_glass",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "invoked_by",
        "reason"
      ],
      "title": "BreakGlassEvent",
      "type": "object"
    },
    "Capability": {
      "description": "A behaviour a model either demonstrably has or does not.\n\nMembership is established by probing, never by a hardcoded table - see\ncore.llm.probe.",
      "enum": [
        "tool_use",
        "json_mode",
        "vision",
        "streaming"
      ],
      "title": "Capability",
      "type": "string"
    },
    "CredentialAction": {
      "description": "Read and write are separate grants.\n\nApproving read never implies write, mirroring the connector split between\ninternal/readonly and internal/write.\n\nNOT_APPLICABLE exists for audit entries that concern no single access -\nbreak-glass and rotation, for instance. It is meaningful only on AuditEntry;\na Grant, Lease or AccessRequest carrying it is invalid, and Phase 3\nvalidation rejects it.\n\nIt is also why AuditEntry.action is not simply nullable: a nullable enum\nemits `anyOf: [$ref, null]`, which go-jsonschema v0.24.1 turns into two\nconflicting UnmarshalJSON methods on the same Go type. Stating \"not\napplicable\" explicitly is clearer than an implicit null convention anyway.",
      "enum": [
        "read",
        "write",
        "not_applicable"
      ],
      "title": "CredentialAction",
      "type": "string"
    },
    "CredentialRef": {
      "additionalProperties": false,
      "description": "A reference to a stored credential. Never the credential.\n\nSafe to persist, to attach to an Investigation and to render in the\ndashboard, because it identifies without disclosing.",
      "properties": {
        "id": {
          "description": "Opaque identifier of the stored credential.",
          "title": "Id",
          "type": "string"
        },
        "name": {
          "description": "Human-readable label, e.g. 'prod-postgres'.",
          "title": "Name",
          "type": "string"
        },
        "scope": {
          "$ref": "#/$defs/CredentialScope"
        },
        "type": {
          "$ref": "#/$defs/CredentialType"
        }
      },
      "required": [
        "id",
        "name",
        "type"
      ],
      "title": "CredentialRef",
      "type": "object"
    },
    "CredentialScope": {
      "additionalProperties": false,
      "description": "Where a credential applies: a server, a service, an environment.",
      "properties": {
        "environment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "e.g. 'prod'. Drives defaults.",
          "title": "Environment"
        },
        "server": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Host or cluster, e.g. 'db-01'.",
          "title": "Server"
        },
        "service": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Logical service, e.g. 'checkout'.",
          "title": "Service"
        }
      },
      "title": "CredentialScope",
      "type": "object"
    },
    "CredentialType": {
      "description": "What kind of credential this is. Governs how the store handles it.",
      "enum": [
        "database",
        "ssh",
        "kubeconfig",
        "http_auth",
        "cloud_key",
        "tls",
        "key_value"
      ],
      "title": "CredentialType",
      "type": "string"
    },
    "Dialect": {
      "description": "Wire format a provider speaks.\n\nNamed by wire format rather than by vendor: a dialect outlives the vendor\nthat popularised it, and several vendors speak each one.\n\n- CHAT_COMPLETIONS: OpenRouter, Groq, Together, DeepSeek, Mistral, vLLM,\n  LM Studio, Ollama, OpenAI and most self-hosted stacks.\n- MESSAGES: Anthropic and API-compatible gateways.\n- GENERATE_CONTENT: Google Gemini.\n- RAW: bespoke HTTP APIs, mapped by configuration.",
      "enum": [
        "chat_completions",
        "messages",
        "generate_content",
        "raw"
      ],
      "title": "Dialect",
      "type": "string"
    },
    "EventEnvelope": {
      "additionalProperties": false,
      "description": "Transport wrapper carrying one event plus its correlation metadata.",
      "properties": {
        "emitted_at": {
          "format": "date-time",
          "title": "Emitted At",
          "type": "string"
        },
        "event": {
          "discriminator": {
            "mapping": {
              "access_requested": "#/$defs/AccessRequestedEvent",
              "approval_requested": "#/$defs/ApprovalRequestedEvent",
              "break_glass": "#/$defs/BreakGlassEvent",
              "finding_produced": "#/$defs/FindingProducedEvent",
              "hypothesis_proposed": "#/$defs/HypothesisProposedEvent",
              "investigation_completed": "#/$defs/InvestigationCompletedEvent",
              "investigation_started": "#/$defs/InvestigationStartedEvent",
              "lease_expired": "#/$defs/LeaseExpiredEvent",
              "step_finished": "#/$defs/StepFinishedEvent",
              "step_started": "#/$defs/StepStartedEvent",
              "trigger_received": "#/$defs/TriggerReceivedEvent",
              "verdict_ready": "#/$defs/VerdictReadyEvent"
            },
            "propertyName": "type"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/TriggerReceivedEvent"
            },
            {
              "$ref": "#/$defs/InvestigationStartedEvent"
            },
            {
              "$ref": "#/$defs/InvestigationCompletedEvent"
            },
            {
              "$ref": "#/$defs/StepStartedEvent"
            },
            {
              "$ref": "#/$defs/StepFinishedEvent"
            },
            {
              "$ref": "#/$defs/FindingProducedEvent"
            },
            {
              "$ref": "#/$defs/HypothesisProposedEvent"
            },
            {
              "$ref": "#/$defs/VerdictReadyEvent"
            },
            {
              "$ref": "#/$defs/ApprovalRequestedEvent"
            },
            {
              "$ref": "#/$defs/AccessRequestedEvent"
            },
            {
              "$ref": "#/$defs/LeaseExpiredEvent"
            },
            {
              "$ref": "#/$defs/BreakGlassEvent"
            }
          ],
          "title": "Event"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "sequence": {
          "default": 0,
          "description": "Monotonic within an investigation. Replay depends on order, so it is carried rather than inferred from arrival.",
          "minimum": 0,
          "title": "Sequence",
          "type": "integer"
        }
      },
      "required": [
        "id",
        "emitted_at",
        "event"
      ],
      "title": "EventEnvelope",
      "type": "object"
    },
    "Evidence": {
      "additionalProperties": false,
      "description": "A single observation, attributable to one connector at one moment.",
      "properties": {
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "observed_at": {
          "description": "When the thing happened, not when it was fetched.",
          "format": "date-time",
          "title": "Observed At",
          "type": "string"
        },
        "payload": {
          "discriminator": {
            "mapping": {
              "k8s_event": "#/$defs/K8sEventPayload",
              "log_cluster": "#/$defs/LogClusterPayload",
              "manifest_diff": "#/$defs/ManifestDiffPayload",
              "metric_window": "#/$defs/MetricWindowPayload",
              "pipeline_run": "#/$defs/PipelineRunPayload"
            },
            "propertyName": "kind"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/MetricWindowPayload"
            },
            {
              "$ref": "#/$defs/LogClusterPayload"
            },
            {
              "$ref": "#/$defs/ManifestDiffPayload"
            },
            {
              "$ref": "#/$defs/K8sEventPayload"
            },
            {
              "$ref": "#/$defs/PipelineRunPayload"
            }
          ],
          "title": "Payload"
        },
        "source": {
          "$ref": "#/$defs/EvidenceSource"
        },
        "subject": {
          "anyOf": [
            {
              "$ref": "#/$defs/ResourceRef"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What this is about, when it is about one thing."
        },
        "summary": {
          "description": "One line a human can read without expanding the payload.",
          "title": "Summary",
          "type": "string"
        }
      },
      "required": [
        "id",
        "source",
        "observed_at",
        "summary",
        "payload"
      ],
      "title": "Evidence",
      "type": "object"
    },
    "EvidenceSource": {
      "additionalProperties": false,
      "description": "Where a piece of Evidence came from, so a human can go and look.",
      "properties": {
        "collected_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When the connector ran, as distinct from what it observed.",
          "title": "Collected At"
        },
        "connector": {
          "description": "Connector that produced it, e.g. 'prometheus'.",
          "title": "Connector",
          "type": "string"
        },
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Query that produced it, verbatim.",
          "title": "Query"
        }
      },
      "required": [
        "connector"
      ],
      "title": "EvidenceSource",
      "type": "object"
    },
    "ExecutionState": {
      "description": "What actually happened, as distinct from what was permitted.",
      "enum": [
        "proposed",
        "dry_run",
        "executing",
        "succeeded",
        "failed",
        "rolled_back",
        "skipped"
      ],
      "title": "ExecutionState",
      "type": "string"
    },
    "Finding": {
      "additionalProperties": false,
      "description": "One agent's supported claim about what it observed.",
      "properties": {
        "agent": {
          "description": "Codename of the agent that produced it, e.g. 'argus'.",
          "title": "Agent",
          "type": "string"
        },
        "confidence": {
          "description": "The agent's own confidence, 0 to 1.",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Confidence",
          "type": "number"
        },
        "detected_at": {
          "format": "date-time",
          "title": "Detected At",
          "type": "string"
        },
        "evidence": {
          "description": "Evidence supporting the claim. A Finding with none is inadmissible.",
          "items": {
            "$ref": "#/$defs/Evidence"
          },
          "title": "Evidence",
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/FindingKind",
          "default": "observation"
        },
        "rationale": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why the Evidence supports the claim.",
          "title": "Rationale"
        },
        "related": {
          "description": "Other Findings this one connects. Populated on a CORRELATION; empty elsewhere, because a detector states what it saw and does not decide what else it belongs with.",
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "title": "Related",
          "type": "array"
        },
        "severity": {
          "$ref": "#/$defs/Severity"
        },
        "subject": {
          "anyOf": [
            {
              "$ref": "#/$defs/ResourceRef"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What the claim is about."
        },
        "tags": {
          "description": "Free-form, for grouping.",
          "items": {
            "type": "string"
          },
          "title": "Tags",
          "type": "array"
        },
        "title": {
          "description": "One line, specific enough to act on.",
          "title": "Title",
          "type": "string"
        },
        "window_end": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Window End"
        },
        "window_start": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Start of the period this claim is about.",
          "title": "Window Start"
        }
      },
      "required": [
        "id",
        "agent",
        "title",
        "severity",
        "confidence",
        "detected_at"
      ],
      "title": "Finding",
      "type": "object"
    },
    "FindingKind": {
      "description": "What sort of claim this is.\n\nDEGRADED is the important one: it is how an agent reports that it could not\ndo its job - a lease expired, a connector was unreachable, a budget ran out.\nMaking it a Finding rather than a silent absence is what keeps a partial\ninvestigation visibly partial.",
      "enum": [
        "observation",
        "anomaly",
        "correlation",
        "risk",
        "degraded"
      ],
      "title": "FindingKind",
      "type": "string"
    },
    "FindingProducedEvent": {
      "additionalProperties": false,
      "description": "An agent returned a Finding.",
      "properties": {
        "finding": {
          "$ref": "#/$defs/Finding"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "finding_produced",
          "default": "finding_produced",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "finding"
      ],
      "title": "FindingProducedEvent",
      "type": "object"
    },
    "Grant": {
      "additionalProperties": false,
      "description": "Standing permission for one agent to reach one credential one way.",
      "properties": {
        "action": {
          "$ref": "#/$defs/CredentialAction"
        },
        "agent": {
          "description": "Agent codename the grant applies to, e.g. 'argus'.",
          "title": "Agent",
          "type": "string"
        },
        "credential_ref": {
          "$ref": "#/$defs/CredentialRef"
        },
        "expires_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Set when mode is ALLOW_UNTIL.",
          "title": "Expires At"
        },
        "granted_at": {
          "format": "date-time",
          "title": "Granted At",
          "type": "string"
        },
        "granted_by": {
          "description": "Who approved it.",
          "title": "Granted By",
          "type": "string"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Set when mode is ALLOW_FOR_INVESTIGATION.",
          "title": "Investigation Id"
        },
        "mode": {
          "$ref": "#/$defs/PermissionMode"
        },
        "override_ask_default": {
          "default": false,
          "description": "Explicit override allowing ALLOW_UNTIL on a production or write grant.",
          "title": "Override Ask Default",
          "type": "boolean"
        },
        "revoked_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Set when revoked.",
          "title": "Revoked At"
        }
      },
      "required": [
        "id",
        "agent",
        "credential_ref",
        "action",
        "mode",
        "granted_by",
        "granted_at"
      ],
      "title": "Grant",
      "type": "object"
    },
    "HypothesisProposedEvent": {
      "additionalProperties": false,
      "description": "A candidate explanation entered the running.",
      "properties": {
        "hypothesis": {
          "$ref": "#/$defs/RootCauseHypothesis"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "hypothesis_proposed",
          "default": "hypothesis_proposed",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "hypothesis"
      ],
      "title": "HypothesisProposedEvent",
      "type": "object"
    },
    "HypothesisStatus": {
      "description": "Where a hypothesis stands once the evidence is in.",
      "enum": [
        "proposed",
        "supported",
        "refuted",
        "inconclusive"
      ],
      "title": "HypothesisStatus",
      "type": "string"
    },
    "Investigation": {
      "additionalProperties": false,
      "description": "One end-to-end run, from trigger to Verdict.",
      "properties": {
        "accounting": {
          "description": "What each agent consumed against what it was allowed. One entry per dispatched step, including the steps that degraded - a run that exhausted its budget is the one anybody asks about.",
          "items": {
            "$ref": "#/$defs/AgentAccounting"
          },
          "title": "Accounting",
          "type": "array"
        },
        "audit": {
          "description": "Cerberus credential audit for this run. Safe to expose: every credential here is a CredentialRef, never a value.",
          "items": {
            "$ref": "#/$defs/AuditEntry"
          },
          "title": "Audit",
          "type": "array"
        },
        "completed_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Completed At"
        },
        "created_at": {
          "format": "date-time",
          "title": "Created At",
          "type": "string"
        },
        "findings": {
          "items": {
            "$ref": "#/$defs/Finding"
          },
          "title": "Findings",
          "type": "array"
        },
        "hypotheses": {
          "description": "Working hypotheses, before the Verdict ranks them.",
          "items": {
            "$ref": "#/$defs/RootCauseHypothesis"
          },
          "title": "Hypotheses",
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "plan": {
          "description": "What Zeus decided to ask.",
          "items": {
            "$ref": "#/$defs/PlanStep"
          },
          "title": "Plan",
          "type": "array"
        },
        "resolutions": {
          "description": "Every Delphi model resolution made during this run, in order.",
          "items": {
            "$ref": "#/$defs/ResolutionRecord"
          },
          "title": "Resolutions",
          "type": "array"
        },
        "scenario": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Simulator scenario that produced this run, when triggered by one. Present so a run can be scored against known ground truth.",
          "title": "Scenario"
        },
        "started_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Started At"
        },
        "state": {
          "$ref": "#/$defs/InvestigationState"
        },
        "tenant": {
          "default": "default",
          "description": "Who this run belongs to. Never empty - see api/auth/dependencies.py.",
          "minLength": 1,
          "title": "Tenant",
          "type": "string"
        },
        "trigger": {
          "$ref": "#/$defs/Trigger"
        },
        "verdict": {
          "anyOf": [
            {
              "$ref": "#/$defs/Verdict"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Absent until the run reaches a conclusion."
        }
      },
      "required": [
        "id",
        "state",
        "trigger",
        "created_at"
      ],
      "title": "Investigation",
      "type": "object"
    },
    "InvestigationCompletedEvent": {
      "additionalProperties": false,
      "description": "A run reached a terminal state, successfully or not.",
      "properties": {
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "partial": {
          "default": false,
          "description": "True when any agent reported DEGRADED.",
          "title": "Partial",
          "type": "boolean"
        },
        "state": {
          "description": "The terminal InvestigationState value.",
          "title": "State",
          "type": "string"
        },
        "type": {
          "const": "investigation_completed",
          "default": "investigation_completed",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "state"
      ],
      "title": "InvestigationCompletedEvent",
      "type": "object"
    },
    "InvestigationStartedEvent": {
      "additionalProperties": false,
      "description": "An Investigation moved out of PENDING.",
      "properties": {
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "investigation_started",
          "default": "investigation_started",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id"
      ],
      "title": "InvestigationStartedEvent",
      "type": "object"
    },
    "InvestigationState": {
      "description": "Lifecycle of an Investigation.",
      "enum": [
        "pending",
        "planning",
        "running",
        "awaiting_approval",
        "completed",
        "failed",
        "cancelled"
      ],
      "title": "InvestigationState",
      "type": "string"
    },
    "K8sEventPayload": {
      "additionalProperties": false,
      "description": "A Kubernetes event, which is often the shortest path to the answer.",
      "properties": {
        "count": {
          "default": 1,
          "minimum": 1,
          "title": "Count",
          "type": "integer"
        },
        "event_type": {
          "default": "Warning",
          "description": "'Normal' or 'Warning'.",
          "title": "Event Type",
          "type": "string"
        },
        "first_seen": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "First Seen"
        },
        "kind": {
          "const": "k8s_event",
          "default": "k8s_event",
          "title": "Kind",
          "type": "string"
        },
        "last_seen": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Last Seen"
        },
        "message": {
          "title": "Message",
          "type": "string"
        },
        "reason": {
          "description": "e.g. 'OOMKilling', 'Unhealthy', 'FailedScheduling'.",
          "title": "Reason",
          "type": "string"
        },
        "target": {
          "$ref": "#/$defs/ResourceRef"
        }
      },
      "required": [
        "target",
        "reason",
        "message"
      ],
      "title": "K8sEventPayload",
      "type": "object"
    },
    "Lease": {
      "additionalProperties": false,
      "description": "Permission to use a credential, bound to one connector and one run.\n\nA lease is not a credential. It is redeemable only by the named connector,\nonly for the named investigation, and only until it expires - so a leaked\nlease is worthless anywhere else.",
      "properties": {
        "action": {
          "$ref": "#/$defs/CredentialAction"
        },
        "connector": {
          "description": "The only connector that may redeem this lease.",
          "title": "Connector",
          "type": "string"
        },
        "credential_ref": {
          "$ref": "#/$defs/CredentialRef"
        },
        "expires_at": {
          "format": "date-time",
          "title": "Expires At",
          "type": "string"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "issued_at": {
          "format": "date-time",
          "title": "Issued At",
          "type": "string"
        },
        "renewable": {
          "default": true,
          "description": "Auto-renews while the underlying grant is valid and the run is live.",
          "title": "Renewable",
          "type": "boolean"
        },
        "renewed_count": {
          "default": 0,
          "minimum": 0,
          "title": "Renewed Count",
          "type": "integer"
        },
        "request_id": {
          "format": "uuid",
          "title": "Request Id",
          "type": "string"
        }
      },
      "required": [
        "id",
        "request_id",
        "investigation_id",
        "connector",
        "credential_ref",
        "action",
        "issued_at",
        "expires_at"
      ],
      "title": "Lease",
      "type": "object"
    },
    "LeaseExpiredEvent": {
      "additionalProperties": false,
      "description": "A lease could not be renewed, so the work behind it stopped.\n\nADR 0005: this must surface as a Finding and never be swallowed. `reason`\ndistinguishes the two cases, because they call for opposite responses -\nan expired grant warrants offering re-approval, a revoked one must not,\nsince re-prompting would undo a deliberate revocation mid-incident.",
      "properties": {
        "agent": {
          "title": "Agent",
          "type": "string"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "lease_id": {
          "format": "uuid",
          "title": "Lease Id",
          "type": "string"
        },
        "reason": {
          "default": "expired",
          "enum": [
            "expired",
            "revoked"
          ],
          "title": "Reason",
          "type": "string"
        },
        "type": {
          "const": "lease_expired",
          "default": "lease_expired",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "lease_id",
        "agent"
      ],
      "title": "LeaseExpiredEvent",
      "type": "object"
    },
    "LogClusterPayload": {
      "additionalProperties": false,
      "description": "A group of log lines sharing a template, plus how surprising it is.",
      "properties": {
        "first_seen": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "First Seen"
        },
        "kind": {
          "const": "log_cluster",
          "default": "log_cluster",
          "title": "Kind",
          "type": "string"
        },
        "last_seen": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Last Seen"
        },
        "novelty": {
          "anyOf": [
            {
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "1.0 means never seen before this window.",
          "title": "Novelty"
        },
        "occurrences": {
          "default": 0,
          "minimum": 0,
          "title": "Occurrences",
          "type": "integer"
        },
        "sample_lines": {
          "description": "Verbatim examples. Redacted before emission.",
          "items": {
            "type": "string"
          },
          "title": "Sample Lines",
          "type": "array"
        },
        "template": {
          "description": "Normalised line with variables masked.",
          "title": "Template",
          "type": "string"
        }
      },
      "required": [
        "template"
      ],
      "title": "LogClusterPayload",
      "type": "object"
    },
    "ManifestDiffPayload": {
      "additionalProperties": false,
      "description": "A change to a manifest or IaC definition, and what it touches.",
      "properties": {
        "changed_fields": {
          "items": {
            "type": "string"
          },
          "title": "Changed Fields",
          "type": "array"
        },
        "diff": {
          "description": "Unified diff.",
          "title": "Diff",
          "type": "string"
        },
        "kind": {
          "const": "manifest_diff",
          "default": "manifest_diff",
          "title": "Kind",
          "type": "string"
        },
        "revision_after": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Revision After"
        },
        "revision_before": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Revision Before"
        },
        "target": {
          "$ref": "#/$defs/ResourceRef"
        }
      },
      "required": [
        "target",
        "diff"
      ],
      "title": "ManifestDiffPayload",
      "type": "object"
    },
    "MetricSample": {
      "additionalProperties": false,
      "description": "One point on a series.",
      "properties": {
        "at": {
          "format": "date-time",
          "title": "At",
          "type": "string"
        },
        "value": {
          "title": "Value",
          "type": "number"
        }
      },
      "required": [
        "at",
        "value"
      ],
      "title": "MetricSample",
      "type": "object"
    },
    "MetricWindowPayload": {
      "additionalProperties": false,
      "description": "A slice of a time series, with the baseline it is being judged against.\n\n`deviation_sigma` is carried rather than recomputed downstream so that the\ndashboard, the verdict and the audit trail all agree on how unusual this\nwas - recomputing invites three different answers.\n\n`scale_floor_engaged` says whether the number can be read as a measurement\nat all. When every sample in a window agrees, MAD is exactly zero and the\nscale falls back to a floor - after which `deviation_sigma` is a property of\nthe floor, not of the distribution. `disk_ratio` over three nodes produced\n1599.63 on a *clean* baseline and 1585.74 as a *signal*: both were the floor\nspeaking. A number whose provenance is the floor must not be\nindistinguishable from one whose provenance is the data.\n\nThe baseline is `centre` and `scale` rather than `mean` and `stddev`, and\nthe estimator is named. Detection here is median/MAD, and writing a median\ninto a field called `baseline_mean` is a number that looks meaningful and is\nnot - the reader would compare it against a mean from somewhere else. The\nprevious fields were removed rather than kept alongside: two estimators side\nby side invites exactly that comparison, and the one on display would be the\none that breaks under contamination.",
      "properties": {
        "baseline_centre": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Middle of the baseline, by `estimator`.",
          "title": "Baseline Centre"
        },
        "baseline_scale": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Spread of the baseline, by `estimator`.",
          "title": "Baseline Scale"
        },
        "deviation_sigma": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Deviations from centre, in units of scale, signed.",
          "title": "Deviation Sigma"
        },
        "estimator": {
          "$ref": "#/$defs/BaselineEstimator",
          "default": "not_applicable",
          "description": "How centre and scale were computed. Must be stated if either is set."
        },
        "kind": {
          "const": "metric_window",
          "default": "metric_window",
          "title": "Kind",
          "type": "string"
        },
        "metric": {
          "description": "Metric name, e.g. 'container_memory_working_set_bytes'.",
          "title": "Metric",
          "type": "string"
        },
        "samples": {
          "items": {
            "$ref": "#/$defs/MetricSample"
          },
          "title": "Samples",
          "type": "array"
        },
        "scale_floor_engaged": {
          "default": false,
          "description": "The scale collapsed and a floor was substituted, so `deviation_sigma` is determined by the floor rather than by the data's own spread.",
          "title": "Scale Floor Engaged",
          "type": "boolean"
        },
        "unit": {
          "default": "",
          "description": "e.g. 'bytes', 'seconds', 'requests/s'.",
          "title": "Unit",
          "type": "string"
        },
        "window_seconds": {
          "default": 0,
          "minimum": 0,
          "title": "Window Seconds",
          "type": "integer"
        }
      },
      "required": [
        "metric"
      ],
      "title": "MetricWindowPayload",
      "type": "object"
    },
    "ModelDescriptor": {
      "additionalProperties": false,
      "description": "One model as observed, not as advertised.\n\nEvery field below the identity pair is measured by core.llm.probe against\nthis deployment, because a model's behaviour behind a particular gateway is\nnot always what its vendor documents.",
      "properties": {
        "capabilities": {
          "description": "Probed, not declared.",
          "items": {
            "$ref": "#/$defs/Capability"
          },
          "title": "Capabilities",
          "type": "array"
        },
        "context_window": {
          "default": 0,
          "minimum": 0,
          "title": "Context Window",
          "type": "integer"
        },
        "input_cost_per_1k": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Input Cost Per 1K"
        },
        "last_probed_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Null means never probed; treat capabilities as unknown.",
          "title": "Last Probed At"
        },
        "median_latency_ms": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Median Latency Ms"
        },
        "model_id": {
          "title": "Model Id",
          "type": "string"
        },
        "output_cost_per_1k": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Output Cost Per 1K"
        },
        "provider_id": {
          "title": "Provider Id",
          "type": "string"
        }
      },
      "required": [
        "provider_id",
        "model_id"
      ],
      "title": "ModelDescriptor",
      "type": "object"
    },
    "ModelRequirements": {
      "additionalProperties": false,
      "description": "What an agent needs from a model. The only thing an agent may declare.",
      "properties": {
        "capabilities": {
          "description": "Capabilities the model must demonstrably have.",
          "items": {
            "$ref": "#/$defs/Capability"
          },
          "title": "Capabilities",
          "type": "array"
        },
        "max_cost_per_call": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Ceiling for one call. Enforced via core.guardrails.budget.",
          "title": "Max Cost Per Call"
        },
        "min_context": {
          "default": 0,
          "description": "Minimum context window in tokens.",
          "minimum": 0,
          "title": "Min Context",
          "type": "integer"
        },
        "tier": {
          "$ref": "#/$defs/Tier",
          "default": "balanced"
        }
      },
      "title": "ModelRequirements",
      "type": "object"
    },
    "PermissionMode": {
      "description": "How a grant answers a request.\n\nALLOW_UNTIL is refused for production targets and for any write action\nunless an explicit override is set - see core.cerberus.policy.defaults.",
      "enum": [
        "deny",
        "ask_each_time",
        "allow_for_investigation",
        "allow_until"
      ],
      "title": "PermissionMode",
      "type": "string"
    },
    "PipelineRunPayload": {
      "additionalProperties": false,
      "description": "One CI pipeline run and the jobs that failed in it.",
      "properties": {
        "commit_sha": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Commit Sha"
        },
        "duration_seconds": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Duration Seconds"
        },
        "failed_jobs": {
          "items": {
            "type": "string"
          },
          "title": "Failed Jobs",
          "type": "array"
        },
        "kind": {
          "const": "pipeline_run",
          "default": "pipeline_run",
          "title": "Kind",
          "type": "string"
        },
        "pipeline_id": {
          "title": "Pipeline Id",
          "type": "string"
        },
        "project": {
          "title": "Project",
          "type": "string"
        },
        "ref": {
          "description": "Branch or tag.",
          "title": "Ref",
          "type": "string"
        },
        "status": {
          "description": "e.g. 'failed', 'success'.",
          "title": "Status",
          "type": "string"
        }
      },
      "required": [
        "pipeline_id",
        "project",
        "ref",
        "status"
      ],
      "title": "PipelineRunPayload",
      "type": "object"
    },
    "PlanStep": {
      "additionalProperties": false,
      "description": "One agent consultation Zeus intends to make.",
      "properties": {
        "agent": {
          "description": "Agent codename.",
          "title": "Agent",
          "type": "string"
        },
        "depends_on": {
          "description": "Agent codenames whose findings this step needs.",
          "items": {
            "type": "string"
          },
          "title": "Depends On",
          "type": "array"
        },
        "finished_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Finished At"
        },
        "reason": {
          "description": "Why this agent is being asked.",
          "title": "Reason",
          "type": "string"
        },
        "started_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Started At"
        },
        "status": {
          "$ref": "#/$defs/StepStatus",
          "default": "pending"
        }
      },
      "required": [
        "agent",
        "reason"
      ],
      "title": "PlanStep",
      "type": "object"
    },
    "ProviderConfig": {
      "additionalProperties": false,
      "description": "A configured provider. Added from settings, never from code.",
      "properties": {
        "auth_mode": {
          "$ref": "#/$defs/AuthMode",
          "default": "bearer"
        },
        "base_url": {
          "description": "Root URL of the provider's API.",
          "title": "Base Url",
          "type": "string"
        },
        "dialect": {
          "$ref": "#/$defs/Dialect"
        },
        "display_name": {
          "title": "Display Name",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "id": {
          "description": "Stable identifier, e.g. 'local-ollama'.",
          "title": "Id",
          "type": "string"
        },
        "manual_models": {
          "description": "Model ids entered by hand when enumeration is absent.",
          "items": {
            "type": "string"
          },
          "title": "Manual Models",
          "type": "array"
        },
        "models_endpoint": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path used to enumerate models, when the provider offers one.",
          "title": "Models Endpoint"
        },
        "secret_ref": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Cerberus credential reference. Never the credential itself.",
          "title": "Secret Ref"
        }
      },
      "required": [
        "id",
        "display_name",
        "dialect",
        "base_url"
      ],
      "title": "ProviderConfig",
      "type": "object"
    },
    "ResolutionRecord": {
      "additionalProperties": false,
      "description": "Why Delphi chose the model it chose, for one call.\n\nAttached to the Investigation so a run is reproducible and can explain its\nown cost without being re-run.",
      "properties": {
        "chosen": {
          "$ref": "#/$defs/ModelDescriptor"
        },
        "estimated_cost": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Estimated Cost"
        },
        "fallback_used": {
          "default": false,
          "title": "Fallback Used",
          "type": "boolean"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "matched_step": {
          "$ref": "#/$defs/ResolutionStep"
        },
        "rejected": {
          "description": "Human-readable reason per rejected candidate, in evaluation order.",
          "items": {
            "type": "string"
          },
          "title": "Rejected",
          "type": "array"
        },
        "requested_by": {
          "description": "Agent codename that consulted Delphi, e.g. 'hermes'.",
          "title": "Requested By",
          "type": "string"
        },
        "requirements": {
          "$ref": "#/$defs/ModelRequirements"
        },
        "resolved_at": {
          "format": "date-time",
          "title": "Resolved At",
          "type": "string"
        }
      },
      "required": [
        "id",
        "requested_by",
        "requirements",
        "matched_step",
        "chosen",
        "resolved_at"
      ],
      "title": "ResolutionRecord",
      "type": "object"
    },
    "ResolutionStep": {
      "description": "Which rung of the resolution cascade produced the answer.",
      "enum": [
        "task_override",
        "agent_binding",
        "tier_default",
        "global_default"
      ],
      "title": "ResolutionStep",
      "type": "string"
    },
    "ResourceRef": {
      "additionalProperties": false,
      "description": "What a piece of Evidence is about.\n\nDeliberately not Kubernetes-shaped: a pipeline and a database are subjects\ntoo, and forcing them into `namespace/kind/name` would be a lie that costs\nan adapter at every call site.",
      "properties": {
        "cluster": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cluster"
        },
        "kind": {
          "description": "e.g. 'deployment', 'pipeline', 'node'.",
          "title": "Kind",
          "type": "string"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "namespace": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where applicable.",
          "title": "Namespace"
        }
      },
      "required": [
        "kind",
        "name"
      ],
      "title": "ResourceRef",
      "type": "object"
    },
    "RootCauseCategory": {
      "description": "The closed vocabulary shared by agents, verdicts and scenario ground truth.\n\nAdding a member is a deliberate act: it widens what an agent may conclude\nand what a scenario may assert. `UNKNOWN` exists so that \"we do not know\" is\na statable conclusion rather than an absent one - an investigation that\ncannot say it will invent something instead.",
      "enum": [
        "memory_leak",
        "resource_contention",
        "bad_deployment",
        "config_error",
        "disk_exhaustion",
        "capacity_saturation",
        "dependency_failure",
        "network_partition",
        "flaky_test",
        "data_corruption",
        "external_incident",
        "unknown"
      ],
      "title": "RootCauseCategory",
      "type": "string"
    },
    "RootCauseHypothesis": {
      "additionalProperties": false,
      "description": "One candidate explanation, and how well it survived contact with evidence.",
      "properties": {
        "category": {
          "$ref": "#/$defs/RootCauseCategory"
        },
        "confidence": {
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Confidence",
          "type": "number"
        },
        "contradicting_finding_ids": {
          "description": "Recorded deliberately. A hypothesis with none listed has usually not been tested, rather than survived testing.",
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "title": "Contradicting Finding Ids",
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "proposed_by": {
          "description": "Agent codename, or 'zeus' for an aggregated one.",
          "title": "Proposed By",
          "type": "string"
        },
        "reasoning": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why the evidence implies this.",
          "title": "Reasoning"
        },
        "statement": {
          "description": "One sentence a human can act on, e.g. 'checkout leaks connections under retry storms, exhausting the pool'.",
          "title": "Statement",
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/HypothesisStatus",
          "default": "proposed"
        },
        "subject": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What it is about, e.g. 'deployment/checkout'.",
          "title": "Subject"
        },
        "supporting_finding_ids": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "title": "Supporting Finding Ids",
          "type": "array"
        }
      },
      "required": [
        "id",
        "category",
        "statement",
        "confidence",
        "proposed_by"
      ],
      "title": "RootCauseHypothesis",
      "type": "object"
    },
    "Severity": {
      "description": "How much a Finding should worry the on-call engineer.",
      "enum": [
        "info",
        "low",
        "medium",
        "high",
        "critical"
      ],
      "title": "Severity",
      "type": "string"
    },
    "StepFinishedEvent": {
      "additionalProperties": false,
      "description": "An agent returned, with or without findings.",
      "properties": {
        "agent": {
          "title": "Agent",
          "type": "string"
        },
        "finding_count": {
          "default": 0,
          "minimum": 0,
          "title": "Finding Count",
          "type": "integer"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "step_finished",
          "default": "step_finished",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "agent"
      ],
      "title": "StepFinishedEvent",
      "type": "object"
    },
    "StepStartedEvent": {
      "additionalProperties": false,
      "description": "Zeus dispatched an agent.",
      "properties": {
        "agent": {
          "title": "Agent",
          "type": "string"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "step_started",
          "default": "step_started",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "agent"
      ],
      "title": "StepStartedEvent",
      "type": "object"
    },
    "StepStatus": {
      "description": "What happened to one dispatched agent.\n\nCOMPLETE with no findings is a real result - the agent looked and saw\nnothing. DEGRADED means it could not look. SKIPPED means it was never\ndispatched. Collapsing any of these into \"no findings\" makes a clean run and\na never-run agent the same number, which is precisely the distinction agent\nscoring depends on.",
      "enum": [
        "pending",
        "running",
        "complete",
        "degraded",
        "skipped"
      ],
      "title": "StepStatus",
      "type": "string"
    },
    "Tier": {
      "description": "Cost/capability band an agent asks for, rather than a specific model.",
      "enum": [
        "cheap",
        "balanced",
        "frontier"
      ],
      "title": "Tier",
      "type": "string"
    },
    "Trigger": {
      "additionalProperties": false,
      "description": "The inbound event that started everything.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/TriggerKind"
        },
        "payload": {
          "additionalProperties": true,
          "description": "Verbatim, unparsed.",
          "title": "Payload",
          "type": "object"
        },
        "received_at": {
          "format": "date-time",
          "title": "Received At",
          "type": "string"
        },
        "source": {
          "description": "Who sent it, e.g. 'alertmanager'.",
          "title": "Source",
          "type": "string"
        },
        "title": {
          "default": "",
          "description": "One line, as the source described it.",
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "received_at",
        "source"
      ],
      "title": "Trigger",
      "type": "object"
    },
    "TriggerKind": {
      "description": "What set an Investigation off.",
      "enum": [
        "alert",
        "webhook",
        "schedule",
        "human_question",
        "simulation"
      ],
      "title": "TriggerKind",
      "type": "string"
    },
    "TriggerReceivedEvent": {
      "additionalProperties": false,
      "description": "An inbound trigger was accepted and an Investigation created for it.\n\nDistinct from `investigation_started`, which marks the run leaving PENDING.\nA webhook can be accepted seconds before anything plans it, and collapsing\nthe two would lose the gap where a backlog becomes visible.",
      "properties": {
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "trigger": {
          "$ref": "#/$defs/Trigger"
        },
        "type": {
          "const": "trigger_received",
          "default": "trigger_received",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "investigation_id",
        "trigger"
      ],
      "title": "TriggerReceivedEvent",
      "type": "object"
    },
    "UIActionResponse": {
      "additionalProperties": false,
      "description": "A user's response to an action, travelling back over AG-UI.\n\nMirrors A2UI's client action message. Carries no decision authority of its\nown: an approval reaching the Approval Gate, or an access decision reaching\nCerberus, is re-validated there against the request it claims to answer.",
      "properties": {
        "action_name": {
          "title": "Action Name",
          "type": "string"
        },
        "context": {
          "additionalProperties": true,
          "title": "Context",
          "type": "object"
        },
        "investigation_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Investigation Id"
        },
        "source_component_id": {
          "title": "Source Component Id",
          "type": "string"
        },
        "surface_id": {
          "format": "uuid",
          "title": "Surface Id",
          "type": "string"
        }
      },
      "required": [
        "surface_id",
        "source_component_id",
        "action_name"
      ],
      "title": "UIActionResponse",
      "type": "object"
    },
    "Verdict": {
      "additionalProperties": false,
      "description": "The orchestrator's ranked conclusion for one Investigation.",
      "properties": {
        "confidence": {
          "description": "Confidence in the leading hypothesis.",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Confidence",
          "type": "number"
        },
        "contributing_findings": {
          "items": {
            "$ref": "#/$defs/Finding"
          },
          "title": "Contributing Findings",
          "type": "array"
        },
        "decided_at": {
          "format": "date-time",
          "title": "Decided At",
          "type": "string"
        },
        "hypotheses": {
          "description": "Ranked most-likely first. Empty means no explanation was reached, which is a legitimate outcome and must not be dressed up as one.",
          "items": {
            "$ref": "#/$defs/RootCauseHypothesis"
          },
          "title": "Hypotheses",
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "recommended_actions": {
          "items": {
            "$ref": "#/$defs/Action"
          },
          "title": "Recommended Actions",
          "type": "array"
        },
        "steps": {
          "description": "What actually ran. REQUIRED, and deliberately not defaulted: a verdict formed without knowing which agents completed is a verdict that cannot tell 'nobody found anything' from 'nobody looked'.",
          "items": {
            "$ref": "#/$defs/PlanStep"
          },
          "title": "Steps",
          "type": "array"
        },
        "summary": {
          "description": "What happened, in one paragraph, for a human.",
          "title": "Summary",
          "type": "string"
        }
      },
      "required": [
        "id",
        "investigation_id",
        "summary",
        "confidence",
        "decided_at",
        "steps"
      ],
      "title": "Verdict",
      "type": "object"
    },
    "VerdictReadyEvent": {
      "additionalProperties": false,
      "description": "The aggregator reached a conclusion.",
      "properties": {
        "investigation_id": {
          "format": "uuid",
          "title": "Investigation Id",
          "type": "string"
        },
        "type": {
          "const": "verdict_ready",
          "default": "verdict_ready",
          "title": "Type",
          "type": "string"
        },
        "verdict": {
          "$ref": "#/$defs/Verdict"
        }
      },
      "required": [
        "investigation_id",
        "verdict"
      ],
      "title": "VerdictReadyEvent",
      "type": "object"
    }
  }
}
