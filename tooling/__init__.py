"""Build tooling that runs through Python rather than a shell.

Same shape as `codegen/`: a top-level package invoked with `python -m`, holding
what the Makefile needs and cannot express portably.

Phase: 4 - Delivery Flow
"""
