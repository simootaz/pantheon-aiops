# Raw measurements

The JSON each scoring in this directory cites, committed rather than left in a
session scratchpad. A scoring that cites data nobody can open is an assertion,
and `tests/unit/test_prediction_records.py` fails the build on one.

Nothing here yet: [`01-template-recovery.md`](../01-template-recovery.md) is
committed and unmeasured, which is the whole point of writing it first.

| file | produced by | cited from |
|---|---|---|
| _(none yet)_ | | |
