{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pantheon.serviceAccountName" . }}
  labels:
    {{- include "pantheon.labels" . | nindent 4 }}
{{- end }}
