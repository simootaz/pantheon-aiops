# Kustomize dev overlay.
#
# Phase: 6 - Go Port & Platform Binaries
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pantheon-dev

resources:
  - ../../base

images:
  - name: ghcr.io/simootaz/pantheon
    newTag: dev

replicas:
  - name: pantheon-api
    count: 1

# TODO: Phase 6 - add environment patches once the base is rendered
patches: []
