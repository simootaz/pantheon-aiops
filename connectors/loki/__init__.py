"""Loki connector: LogQL queries and label discovery for log retrieval.

Phase: 2 - Orchestrator & Investigation Flow
"""

# The server is connectors/loki/server.py, run as `python -m connectors.loki.server`.
# Nothing imports it as a package attribute, so there is nothing to re-export.
